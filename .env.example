# Monitorix (agency-dashboard) — verejné, RLS-chránené hodnoty pre build-time fetch.
# Bez nich build funguje (použije commitnutý src/data/monitorix.json fallback),
# len nenatiahne živé čísla. V CI sú nastavené ako GitHub Actions secrets.
# Skopíruj do `.env` (gitignored) pre lokálny dev so živými dátami.
PUBLIC_MONITORIX_URL=https://ssevintoefkxovissoux.supabase.co
PUBLIC_MONITORIX_ANON_KEY=
