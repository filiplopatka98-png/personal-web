---
import BaseLayout from '../layouts/BaseLayout.astro';
import { t } from '../i18n';

const lang = 'sk';
---
<BaseLayout lang={lang} canonicalPath="/sluzby/" altLangUrl="/en/services/" title={`${t('nav.services', lang)} — Filip Lopatka`}>
  <section style="padding: 96px 56px; max-width: 880px; margin: 0 auto;">
    <p style="font-family: var(--font-mono); font-size: 12px; letter-spacing: 0.08em; text-transform: uppercase; opacity: 0.5; margin-bottom: 24px;">02 — {t('nav.services', lang)}</p>
    <h1 style="font-family: var(--font-display); font-size: clamp(56px, 8vw, 96px); line-height: 0.95; letter-spacing: -0.02em; font-weight: 400; margin: 0;">
      Služby — <em>v príprave</em>.
    </h1>
    <p style="margin-top: 32px; font-size: 18px; opacity: 0.7; max-width: 600px;">
      Plný obsah pripravujem. Zatiaľ ma môžeš osloviť cez brief alebo formulár v päte.
    </p>
  </section>
</BaseLayout>
